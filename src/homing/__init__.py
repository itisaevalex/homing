"""homing — make a personal laptop legible to agents.

Walks ``$HOME``, classifies what it finds, and writes a parallel structured
representation under ``~/system/`` that any agent can read to understand the
machine without re-discovering everything from scratch.
"""

__version__ = "0.1.0"
